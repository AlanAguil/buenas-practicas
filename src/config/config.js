export const CONFIG = {
    maxRegistros: 1000,
    adminEmail: "admin@sistema.com", // process.env.ADMIN_EMAIL, 
    adminPassword: "PlaceholderPassword", // process.env.ADMIN_PASSWORD,
    debugMode: true, // process.env.DEBUG_MODE,
    serverIP: "127.0.0.1" // process.env.SERVER_IP
};

export default CONFIG;  